---
phase: 01-3d-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/gl_context.h
  - src/gl_context.c
  - src/renderer_3d.h
  - src/renderer_3d.c
  - assets/shaders/tile.vert
  - assets/shaders/tile.frag
  - Makefile
  - src/main_stub.c
requirements:
  - VIS-01
  - VIS-02
  - VIS-05
  - PLAT-01
  - PLAT-02
autonomous: true
must_haves:
  truths:
    - "D-01: paredes com tijolos 3D individuais visiveis"
    - "D-04: alvos como marcadores 2D no chao plano"
    - "D-09: shading sutil faces claras e escuras sem sombras projetadas"
    - "D-10: paredes tom tijolo avermelhado marrom C_BRICK"
    - "D-11: alvos pontos ciano brilhantes sobre piso escuro"
    - "Tabuleiro renderizado em perspectiva isometrica 3D top-down"
    - "Viewport usa SDL_GL_GetDrawableSize para HiDPI"
  artifacts:
    - src/gl_context.c
    - src/renderer_3d.c
    - assets/shaders/tile.vert
    - assets/shaders/tile.frag
  key_links:
    - "main chama r3d_init/r3d_draw_board no loop SC_PLAY"
    - "Shaders compilam com #version 330 core"
---

<objective>
Substituir render 2D por pipeline OpenGL 3.3: contexto GL, shaders, camera isometrica.

Decisoes: D-01 tijolos individuais, D-04 alvos 2D no chao, D-09 shading sutil, D-10 tom tijolo avermelhado, D-11 pontos ciano brilhantes.

Purpose: Primeiro slice 3D visivel — tabuleiro isometrico com paredes fieis ao video de referencia.
Output: gl_context + renderer_3d desenhando paredes, chao e alvos; jogo ainda jogavel (caixas/personagem podem ser placeholders geometricos simples neste plano se necessario, mas paredes DEVEM ser tijolos individuais).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-3d-foundation/01-CONTEXT.md
@.planning/phases/01-3d-foundation/01-UI-SPEC.md
@.planning/phases/01-3d-foundation/01-RESEARCH.md
@.planning/phases/01-3d-foundation/01-PATTERNS.md
@src/game_logic.h
@src/main_stub.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: gl_context — SDL OpenGL + shaders 330 core</name>
  <files>src/gl_context.h, src/gl_context.c, assets/shaders/tile.vert, assets/shaders/tile.frag, Makefile</files>
  <read_first>.planning/phases/01-3d-foundation/01-RESEARCH.md, Makefile, src/main_stub.c</read_first>
  <acceptance_criteria>
    - SDL_Window criado com SDL_WINDOW_OPENGL flag
    - SDL_GL_SetAttribute: PROFILE_CORE, CONTEXT_MAJOR_VERSION 3, MINOR 3
    - Shaders tile.vert e tile.frag usam #version 330 core
    - gl_context compila/linka shaders; stderr+exit em falha (padrao sokoban.c)
    - VAO/VBO cubo unitario criado para reutilizacao
    - Makefile linka OpenGL: -framework OpenGL (Darwin), -lGL (Linux)
    - GLAD2 ou loader equivalente incluido (single-file header)
  </acceptance_criteria>
  <action>
    Criar assets/shaders/tile.vert com MVP uniform (mat4 u_mvp) e atributo vec3 a_pos.
    Criar assets/shaders/tile.frag com uniform vec3 u_color para cor por face (D-09 shading sutil via cores diferentes passadas pelo CPU).
    Implementar gl_context.c: gl_init(SDL_Window*), gl_load_shaders(path_vert, path_frag), gl_create_unit_cube_vao(), gl_shutdown().
    Adicionar GLAD2 (vendor/glad.c + glad.h ou inline) ao Makefile.
    Atualizar main_stub.c: SDL_CreateWindow com SDL_WINDOW_OPENGL, SDL_GL_CreateContext, SDL_GL_SetSwapInterval(1).
    Chamar SDL_GL_GetDrawableSize para viewport dimensions (PLAT-02, D-HiDPI per STATE.md).
    Remover SDL_CreateRenderer — nao misturar SDL_Renderer com GL (PITFALLS anti-pattern 1).
  </action>
  <verify>
    <automated>make clean && make 2>&1; test -f assets/shaders/tile.vert && grep -q '330 core' assets/shaders/tile.vert && echo SHADERS_OK</automated>
  </verify>
  <done>Contexto OpenGL inicializa e shaders compilam em macOS</done>
</task>

<task type="auto">
  <name>Task 2: renderer_3d — camera isometrica e draw_wall_3d com tijolos</name>
  <files>src/renderer_3d.h, src/renderer_3d.c</files>
  <read_first>.planning/phases/01-3d-foundation/01-UI-SPEC.md, src/game_logic.h, src/gl_context.h</read_first>
  <acceptance_criteria>
    - r3d_init, r3d_begin_frame, r3d_draw_board, r3d_end_frame, r3d_shutdown declarados
    - Projecao ortografica isometrica: rotacao Y ~45 graus, inclinacao X ~30 graus (VIS-01)
    - draw_wall_3d renderiza multiplos blocos 3D visiveis por celula de parede — NAO superficie plana texturizada (D-01)
    - Cores parede: C_BRICK face superior, C_DKBRICK faces laterais (D-10)
    - draw_floor_3d: plano escuro C_BLACK dominante
    - draw_goal_marker: circulo/ponto ciano brilhante C_CYAN no chao plano (D-04, D-11)
    - Painter's Algorithm: sort tiles por (col+row) antes de desenhar (STATE.md)
    - renderer_3d inclui game_logic.h mas NAO chama move_player (PITFALL 8)
  </acceptance_criteria>
  <action>
    Implementar src/renderer_3d.c per RESEARCH.md Pattern 3.
    draw_wall_3d: subdividir face frontal da parede em grid de mini-cubos/tijolos 3D separados (detalhe geometrico e discrecao D-01 — minimo 2x2 blocos visiveis por tile).
    r3d_draw_board(Board* b): iterar grid, dispatch por char tile; paredes, chao, alvos neste plano.
    Caixas e jogador: cubos placeholder simples (C_GOLD/C_MAGENTA) aceitavel neste plano — plan 03 refina para engradado/blocky.
    Calcular matriz MVP por tile a partir de grid (x,y) para posicionamento isometrico.
  </action>
  <verify>
    <automated>grep -c 'draw_wall' src/renderer_3d.c | grep -qv '^0$' && grep -c 'C_BRICK' src/renderer_3d.c | grep -qv '^0$'</automated>
  </verify>
  <done>Paredes 3D com tijolos individuais renderizam no tabuleiro isometrico</done>
</task>

<task type="auto">
  <name>Task 3: Integrar renderer no main loop SC_PLAY</name>
  <files>src/main_stub.c</files>
  <read_first>src/renderer_3d.h, src/main_stub.c</read_first>
  <acceptance_criteria>
    - SC_PLAY renderiza via r3d_draw_board em vez de draw_wall/draw_floor SDL rects
    - SDL_GL_SwapWindow no fim do frame
    - Tabuleiro centralizado na area acima do HUD 40px (UI-SPEC layout)
    - Input teclado ainda funciona via game_logic (GAME-01 preservado)
    - Janela abre sem crash em macOS com Retina
  </acceptance_criteria>
  <action>
    Renomear main_stub.c para src/main.c se ainda nao renomeado; atualizar Makefile SRCS.
    No branch SC_PLAY: r3d_begin_frame, calcular ox/oy centralizando tabuleiro, r3d_draw_board(&B, ox, oy), r3d_end_frame, SDL_GL_SwapWindow.
    Manter handle_key inalterado chamando game_logic.
    Reservar faixa inferior 40px para HUD (proximo plano); tabuleiro nao invade HUD area.
  </action>
  <verify>
    <automated>make 2>&1; test -x ./sokoban && echo RUN_OK</automated>
  </verify>
  <done>Jogo abre com tabuleiro 3D isometrico jogavel</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU/Shaders | Shader source local em assets/shaders/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Shader files | accept | Local assets only |
| T-01-SC | Tampering | GL loader | accept | Vendored GLAD, no npm |
</threat_model>

<verification>
- [ ] ./sokoban mostra tabuleiro isometrico 3D
- [ ] Paredes tem tijolos visivelmente separados (D-01)
- [ ] Alvos aparecem como pontos ciano no chao (D-11)
- [ ] SDL_GL_GetDrawableSize usado para viewport (PLAT-02)
</verification>

<success_criteria>
Pipeline OpenGL funcional com paredes 3D isometricas e gameplay preservado.
</success_criteria>

<output>
Create `.planning/phases/01-3d-foundation/01-02-SUMMARY.md` when done
</output>
