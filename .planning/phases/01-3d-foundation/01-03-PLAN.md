---
phase: 01-3d-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/renderer_3d.c
  - src/hud.h
  - src/hud.c
  - src/main.c
  - Makefile
requirements:
  - VIS-03
  - VIS-04
  - VIS-05
  - GAME-05
autonomous: true
must_haves:
  truths:
    - "D-02: caixotes engradado detalhado tabuas douradas marrom"
    - "D-03: personagem humanoide blocky cabeca corpo pernas legivel"
    - "D-09: shading sutil paleta CGA faces claras escuras"
    - "D-12: caixa no alvo verde dourado equivalente BOXG"
    - "D-13: HUD hibrido SDL2_ttf overlay sobre viewport OpenGL"
    - "D-14: HUD barra inferior 40px tabuleiro area central superior"
    - "D-15: estilo HUD retro CGA fundo escuro texto ciano magenta"
    - "HUD exibe fase movimentos empurroes e tempo na barra inferior"
  artifacts:
    - src/hud.c
    - src/renderer_3d.c
  key_links:
    - "hud_draw renderiza barra 40px com texto C_CYAN via SDL2_ttf"
    - "draw_box_3d usa C_GOLD/C_GREEN conforme on_goal"
---

<objective>
Completar tiles 3D e HUD hibrido retro CGA com stats GAME-05.

Decisoes: D-02 engradado, D-03 personagem blocky, D-09 shading sutil, D-12 BOXG verde, D-13 HUD hibrido ttf+GL, D-14 barra inferior, D-15 estilo retro CGA.

Purpose: Experiencia visual completa do tabuleiro + feedback de stats durante partida.
Output: Todos tile types 3D + HUD overlay + overlay SC_WIN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-3d-foundation/01-UI-SPEC.md
@.planning/phases/01-3d-foundation/01-CONTEXT.md
@sokoban.c
@src/renderer_3d.c
@src/main.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: draw_box_3d engradado e draw_player_3d blocky</name>
  <files>src/renderer_3d.c</files>
  <read_first>sokoban.c, .planning/phases/01-3d-foundation/01-UI-SPEC.md, src/game_logic.h</read_first>
  <acceptance_criteria>
    - draw_box_3d: geometria engradado — tabuas visiveis + listras douradas/marrom C_GOLD/C_DKGOLD (D-02, VIS-03)
    - Parametro on_goal: topo C_GREEN/C_DKGRN quando BOXG equivalente (D-12)
    - draw_player_3d: cabeca branca C_WHITE, corpo magenta C_MAGENTA, pernas ciano C_CYAN (D-03, VIS-04)
    - Shading sutil: faces superiores tom base, laterais tom C_DK* (D-09)
    - NAO cubo LEGO generico sem detalhe de engradado
  </acceptance_criteria>
  <action>
    Implementar draw_box_3d e draw_player_3d em renderer_3d.c substituindo placeholders do plan 02.
    Box: compor multiplas quads/cubos finos simulando tabuas horizontais e verticais; bordas C_DKGOLD.
    Player: 3 primitivas empilhadas (head 0.4h, body 0.5h, legs 0.3h) per ARCHITECTURE.md Pattern 3.
    Integrar em r3d_draw_board switch para BOX, BOXG, PLAYER, PLYRG.
    Preservar Painter's Algorithm sort order.
  </action>
  <verify>
    <automated>grep -c 'draw_box_3d\|draw_player_3d' src/renderer_3d.c | grep -qv '^0$'</automated>
  </verify>
  <done>Caixotes e personagem 3D completos conforme UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Modulo HUD hibrido SDL2_ttf overlay</name>
  <files>src/hud.h, src/hud.c, Makefile</files>
  <read_first>sokoban.c, .planning/phases/01-3d-foundation/01-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - hud_init carrega RobotoMono-Bold.ttf 18px e 40px de assets/fonts/ com fallbacks macOS/Linux
    - hud_draw_stats: barra 40px altura, fundo C_DKMAG, borda superior 2px C_MAGENTA (D-14, UI-SPEC)
    - Texto HUD: formato FASE %d/%d movimentos: %04d empurroes: %04d tempo: %u:%02u:%02u em C_CYAN (GAME-05)
    - hud_draw_win_overlay: caixa 460x140 centralizada, titulo FASE COMPLETA! C_GREEN, CTA [ ENTER para a proxima fase ] (UI-SPEC copywriting)
    - Texto renderizado via SDL2_ttf para textura GL ou quad overlay pos-GL — NAO SDL_Renderer
  </acceptance_criteria>
  <action>
    Extrair open_fonts, draw_text, draw_text_c de sokoban.c para src/hud.c.
    Implementar hud_render_text_to_gl ou equivalente: TTF_RenderText_Blended -> SDL_Surface -> glTexImage2D -> draw quad.
    hud_draw(Scene scene, int win_w, int win_h, Board* b): desenhar barra inferior e overlay SC_WIN.
    Preservar frozen timer logic de sokoban.c linhas 395-397.
  </action>
  <verify>
    <automated>grep -c 'movimentos' src/hud.c | grep -qv '^0$' && grep -c 'C_DKMAG' src/hud.c | grep -qv '^0$'</automated>
  </verify>
  <done>HUD retro CGA renderiza stats e overlay de vitoria</done>
</task>

<task type="auto">
  <name>Task 3: Integrar HUD e paleta CGA completa no loop</name>
  <files>src/main.c</files>
  <read_first>src/hud.h, src/renderer_3d.h, src/main.c</read_first>
  <acceptance_criteria>
    - Frame order: r3d_begin_frame -> r3d_draw_board -> hud_draw -> SDL_GL_SwapWindow
    - SC_PLAY e SC_WIN mostram HUD stats
    - SC_WIN mostra overlay centralizado sobre tabuleiro
    - Paleta CGA preservada na estetica 3D (VIS-05): cores C_* mapeadas nos materiais 3D
    - Tabuleiro ocupa area central superior; HUD nao sobrepoe tiles de forma ilegivel
  </acceptance_criteria>
  <action>
    Atualizar main.c loop de render: chamar hud_draw apos r3d_draw_board em SC_PLAY e SC_WIN.
    Garantir viewport GL limitado a (0, 0, win_w, win_h - HUD) para tabuleiro; HUD desenhado na faixa inferior.
    Verificar contraste alvos C_CYAN vs piso escuro (D-11 legibilidade maxima).
  </action>
  <verify>
    <automated>make 2>&1; grep -c 'hud_draw' src/main.c | grep -qv '^0$'</automated>
  </verify>
  <done>Loop completo: tabuleiro 3D + HUD + overlay vitoria</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Font loading | assets/fonts/ path relativo pos chdir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Font fallback paths | accept | System font fallbacks only |
</threat_model>

<verification>
- [ ] Caixotes parecem engradado madeira/dourado (D-02)
- [ ] Personagem blocky legivel (D-03)
- [ ] Caixa no alvo verde (D-12)
- [ ] HUD mostra fase/movimentos/empurroes/tempo (GAME-05)
- [ ] Overlay SC_WIN com copy correto
</verification>

<success_criteria>
Tabuleiro 3D completo com HUD retro e todos tile types fieis ao contrato UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/01-3d-foundation/01-03-SUMMARY.md` when done
</output>
