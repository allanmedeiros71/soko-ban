---
phase: 01-3d-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/renderer_3d.c
  - src/main.c
  - src/hud.c
  - README.md
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - PLAT-01
  - PLAT-02
  - PLAT-03
autonomous: false
must_haves:
  truths:
    - "D-05: movimento personagem deslize suave 100-150ms entre celulas"
    - "D-06: empurrar caixa anima personagem e caixa juntos"
    - "D-07: input bloqueado durante animacao de movimento"
    - "D-08: undo sempre instantaneo sem animacao reversa"
    - "D-16: cena TITLE pipeline 3D simples com texto overlay"
    - "make funciona em macOS e Linux sem distorcao viewport"
  artifacts:
    - src/renderer_3d.c
    - src/main.c
  key_links:
    - "renderer mantem estado animacao separado de Board grid"
    - "handle_key ignora movimento quando animating=true"
---

<objective>
Animacao de movimento, undo instantaneo, cena TITLE 3D, verificacao dual-platform PLAT-01/02/03.

Decisoes: D-05 deslize suave, D-06 push animado, D-07 bloquear input, D-08 undo instantaneo, D-16 TITLE pipeline 3D.

Purpose: Polish de interacao + consistencia visual TITLE + gate de plataforma antes de fechar Phase 1.
Output: Jogo 3D completo com animacao, TITLE 3D, builds macOS+Linux verificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-3d-foundation/01-CONTEXT.md
@.planning/phases/01-3d-foundation/01-UI-SPEC.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sistema de animacao de movimento no renderer</name>
  <files>src/renderer_3d.h, src/renderer_3d.c, src/main.c</files>
  <read_first>.planning/phases/01-3d-foundation/01-CONTEXT.md, src/game_logic.h</read_first>
  <acceptance_criteria>
    - Struct AnimState no renderer: from_x/y, to_x/y, progress 0-1, duration_ms 100-150 (D-05, discrecao)
    - r3d_start_move(dx, dy, pushed_box bool): inicia animacao visual sem alterar grid ate completar OU grid atualiza imediatamente e renderer interpola posicoes visuais (padrao: grid muda imediato, renderer interpola — undo pula animacao)
    - Empurrar caixa: player e box animam juntos (D-06)
    - r3d_update(dt_ms): avanca progress; retorna animating=true/false
    - Quando animating: main ignora teclas de movimento (D-07)
    - do_undo: sem animacao reversa — posicoes visuais snap instantaneo (D-08)
    - Reiniciar fase R: instantaneo sem animacao
  </acceptance_criteria>
  <action>
    Adicionar AnimState estatico em renderer_3d.c per RESEARCH.md (animacao e estado do renderer, nao game_logic).
    r3d_draw_board usa posicoes interpoladas para player/box durante animacao; grid e source of truth apos movimento commitado.
    Em main.c handle_key: wrapper que chama move_player so quando !r3d_is_animating(); apos move bem-sucedido chamar r3d_start_move.
    do_undo path: chamar r3d_snap_to_grid() resetando AnimState sem lerp.
    Duracao exata dentro 100-150ms — usar 120ms como default documentado.
  </action>
  <verify>
    <automated>grep -c 'AnimState\|r3d_is_animating\|r3d_start_move' src/renderer_3d.c | awk '{print ($1>=3)?"ANIM_OK":"FAIL"}'</automated>
  </verify>
  <done>Animacao suave funcional com input block e undo instantaneo</done>
</task>

<task type="auto">
  <name>Task 2: SC_TITLE com pipeline 3D e copy UI-SPEC</name>
  <files>src/main.c, src/hud.c</files>
  <read_first>.planning/phases/01-3d-foundation/01-UI-SPEC.md, sokoban.c</read_first>
  <acceptance_criteria>
    - SC_TITLE renderiza tabuleiro 3D demo ou fundo 3D simples via mesmo pipeline OpenGL (D-16)
    - Texto overlay: SOKO-BAN C_MAGENTA, subtitulo C_CYAN, instrucoes C_WHITE, CTA [ ENTER para jogar ] C_GOLD — copy exato UI-SPEC
    - Posicoes texto conforme UI-SPEC spacing (titulo y~120, subtitulo y~185, instrucoes y~320/350, CTA y~430)
    - ENTER ou SPACE avanca para SC_PLAY
  </acceptance_criteria>
  <action>
    Implementar render branch SC_TITLE em main.c: r3d_draw_board com level demo ou grid estatico decorativo.
    hud_draw_title(screen_w, screen_h) com strings e cores exatas do UI-SPEC copywriting contract.
    Manter consistencia visual imediata com SC_PLAY (mesmo pipeline GL).
  </action>
  <verify>
    <automated>grep -c 'ENTER para jogar' src/hud.c src/main.c 2>/dev/null | awk '{s+=$1} END {print (s>=1)?"TITLE_OK":"FAIL"}'</automated>
  </verify>
  <done>TITLE usa render 3D + overlay texto retro CGA</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verificacao dual-platform macOS + Linux</name>
  <files>README.md, Makefile</files>
  <read_first>.planning/STATE.md, README.md, Makefile</read_first>
  <acceptance_criteria>
    - make compila sem erro no macOS atual (PLAT-01)
    - make compila no Linux com -lGL (PLAT-03) — verificar flags Makefile
    - SDL_GL_GetDrawableSize usado; viewport nao distorcido em Retina (PLAT-02)
    - README.md atualizado com deps OpenGL e instrucoes build macOS/Linux
    - GAME-01 a GAME-04 re-validados apos animacao
    - Jogador confirma visualmente: tijolos, engradado, blocky, HUD, animacao, undo snappy
  </acceptance_criteria>
  <action>
    Executar make clean && make localmente.
    Revisar Makefile: branch Linux inclui -lGL e -lm; branch Darwin inclui -framework OpenGL.
    Atualizar README.md secao dependencias: libsdl2-dev, libgl1-mesa-dev (Linux), brew sdl2 (macOS).
    Documentar que shaders usam GLSL 330 core.
    Solicitar ao usuario verificacao visual do jogo completo.
  </action>
  <verify>
    <automated>make clean && make 2>&1; grep -q 'OpenGL\|lGL\|framework OpenGL' Makefile && echo MAKEFILE_GL_OK</automated>
    <human-check>
      Abrir ./sokoban e confirmar:
      1. Tabuleiro isometrico 3D com tijolos individuais
      2. Movimento suave ~100-150ms; undo instantaneo
      3. HUD stats visivel na barra inferior
      4. TITLE e PLAY consistentes visualmente
      Responder "approved" ou descrever issues.
    </human-check>
  </verify>
  <done>Phase 1 verificada em macOS; Makefile pronto para Linux; usuario aprovou visual</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Platform GL | Diferencas drivers macOS/Linux |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial | GL init failure | mitigate | stderr + exit com mensagem clara |
</threat_model>

<verification>
- [ ] Animacao movimento 100-150ms (D-05)
- [ ] Push animado junto (D-06)
- [ ] Input blocked durante anim (D-07)
- [ ] Undo instantaneo (D-08)
- [ ] TITLE 3D simples (D-16)
- [ ] make macOS OK (PLAT-01)
- [ ] HiDPI OK (PLAT-02)
- [ ] Linux Makefile flags OK (PLAT-03)
</verification>

<success_criteria>
Phase 1 completa: gameplay 100% preservado, visual 3D isometrico, animacao, HUD, TITLE 3D, dual-platform ready.
</success_criteria>

<output>
Create `.planning/phases/01-3d-foundation/01-04-SUMMARY.md` when done
</output>
