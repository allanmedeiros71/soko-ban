---
phase: 01-3d-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game_logic.h
  - src/game_logic.c
  - Makefile
  - .gitignore
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - PLAT-01
autonomous: true
must_haves:
  truths:
    - "make compila binario sokoban a partir de src/game_logic.c"
    - "Jogador move, empurra, desfaz, reinicia e vence fases via teclado"
    - "game_logic.c nao inclui SDL.h"
  artifacts:
    - src/game_logic.h
    - src/game_logic.c
  key_links:
    - "game_logic expoe Board B e funcoes move_player/load_level/is_won/undo"
---

## Phase Goal

**As a** jogador de Sokoban, **I want to** continuar jogando com a mesma mecanica enquanto o codigo e modularizado, **so that** a migracao 3D nao quebre o gameplay existente.

<objective>
Extrair logica pura de tabuleiro para `src/game_logic.c/.h` e atualizar Makefile para build modular — jogo ainda jogavel (render 2D temporario via sokoban.c ou stub minimo).

Purpose: STATE.md exige extrair game_logic ANTES de OpenGL; um commit, uma mudanca.
Output: `game_logic` isolado, zero SDL, `make` funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-3d-foundation/01-CONTEXT.md
@.planning/phases/01-3d-foundation/01-RESEARCH.md
@.planning/phases/01-3d-foundation/01-PATTERNS.md
@sokoban.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar game_logic.h com API pura</name>
  <files>src/game_logic.h</files>
  <read_first>sokoban.c, .planning/phases/01-3d-foundation/01-PATTERNS.md</read_first>
  <acceptance_criteria>
    - src/game_logic.h existe e nao inclui SDL.h
    - Exporta typedef Color, Board, Snapshot, Scene enum SC_TITLE/SC_PLAY/SC_WIN
    - Exporta defines WALL, FLOOR, GOAL, BOX, BOXG, PLAYER, PLYRG, MAXW, MAXH, NLEVELS, UNDO_MAX
    - Exporta extern Board B, extern int level_index, extern undo_stack/undo_top
    - Declara load_level, is_won, push_undo, do_undo, move_player, is_goal_cell
    - Paleta C_* inclui C_BRICK e C_DKBRICK conforme UI-SPEC
  </acceptance_criteria>
  <action>
    Criar src/game_logic.h extraindo tipos, constantes de tile, paleta C_* (incluindo C_BRICK {180,80,50} e C_DKBRICK {120,45,30} per UI-SPEC), struct Board, undo stack, e declaracoes de funcoes de sokoban.c linhas 24-196.
    Nao incluir SDL.h, SDL_ttf.h, ou qualquer header grafico.
    Manter comentarios de secao em portugues seguindo convencao do projeto.
  </action>
  <verify>
    <automated>grep -L 'SDL' src/game_logic.h && grep -c 'move_player' src/game_logic.h | grep -qv '^0$'</automated>
  </verify>
  <done>Header publico de game_logic existe sem dependencia SDL</done>
</task>

<task type="auto">
  <name>Task 2: Implementar game_logic.c com logica identica</name>
  <files>src/game_logic.c</files>
  <read_first>sokoban.c, src/game_logic.h</read_first>
  <acceptance_criteria>
    - src/game_logic.c implementa load_level, move_player, is_won, push_undo, do_undo, is_goal_cell
    - Array levels[] e dados NLEVELS copiados verbatim de sokoban.c
    - Comportamento identico ao original: colisao, empurrar caixa, undo, deteccao vitoria
    - Apenas headers stdlib: stdio, stdlib, string, stdbool.h — zero SDL
  </acceptance_criteria>
  <action>
    Copiar implementacoes de sokoban.c (load_level, is_won, push_undo, do_undo, is_goal_cell, move_player) para src/game_logic.c.
    Copiar levels[] array completo.
    Inicializar globals B, level_index, undo_stack, undo_top.
    Nao alterar algoritmos — copia fiel per STATE.md big-bang prohibition.
  </action>
  <verify>
    <automated>grep -L 'SDL' src/game_logic.c && grep -c 'levels\[\]' src/game_logic.c | grep -qv '^0$'</automated>
  </verify>
  <done>Logica de tabuleiro compilavel e identica ao sokoban.c original</done>
</task>

<task type="auto">
  <name>Task 3: Makefile modular e .gitignore</name>
  <files>Makefile, .gitignore, src/main_stub.c</files>
  <read_first>Makefile, sokoban.c, src/game_logic.h</read_first>
  <acceptance_criteria>
    - Makefile compila src/main_stub.c + src/game_logic.c em binario sokoban
    - main_stub.c usa SDL2 renderer 2D copiando draw_* e loop de sokoban.c (temporario ate plan 02)
    - make run inicia jogo jogavel: mover WASD, undo U, restart R, win detection
    - .gitignore contem sokoban e *.o
    - make clean remove binario e objetos
    - PLAT-01: compila no macOS atual
  </acceptance_criteria>
  <action>
    Criar src/main_stub.c: incluir game_logic.h, copiar helpers draw_*, open_fonts, handle_key, main loop 2D de sokoban.c linhas 119-421.
    Atualizar Makefile: SRCS=src/main_stub.c src/game_logic.c, preservar logica Darwin/Linux existente para SDL2 flags.
    Adicionar .gitignore com sokoban e *.o se nao existir.
    Manter sokoban.c intacto como referencia (target legado opcional ou comentado).
    Adicionar SDL_GetBasePath/chdir no inicio de main para assets/fonts funcionarem fora do cwd (PITFALLS #6).
  </action>
  <verify>
    <automated>make clean && make 2>&1 | tail -1; test -x ./sokoban && echo BUILD_OK</automated>
  </verify>
  <done>make produz sokoban jogavel em 2D usando game_logic modular</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O | Apenas levels hardcoded — sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Makefile deps | accept | Build local only, no network |
| T-01-SC | Tampering | make install | accept | No package installs in this plan |
</threat_model>

<verification>
- [ ] make compila sem warnings criticos
- [ ] ./sokoban abre janela e tabuleiro 2D aparece
- [ ] GAME-01: mover 4 direcoes e empurrar caixa funciona
- [ ] GAME-02: undo restaura estado
- [ ] GAME-03: R reinicia fase
- [ ] GAME-04: vitória detectada com todas caixas nos alvos
</verification>

<success_criteria>
game_logic isolado compila e jogo 2D modular e jogavel via make.
</success_criteria>

<output>
Create `.planning/phases/01-3d-foundation/01-01-SUMMARY.md` when done
</output>
